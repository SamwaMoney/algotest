import java.util.*;


public class ScoringAlgo {
    public static void main(String[] args) {
        ArrayList<Integer> good = new ArrayList<>();
        ArrayList<Integer> bad = new ArrayList<>();
        ArrayList<Integer> special = new ArrayList<>();

        AllClassAlgo allAlgo = new AllClassAlgo();
        WeekdayAlgo weekAlgo = new WeekdayAlgo();
        //Object[] allResult = allAlgo.allClassAlgo(table, move, good, bad, special);
        //Object[] weekResult = weekAlgo.weekdayAlgo(table, move, good, bad, special);
    }

}